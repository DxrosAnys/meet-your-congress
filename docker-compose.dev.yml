version: "3.8"

services:
  app:
    container_name: meet-your-congress-app-dev
    image: meet-your-congress-app-dev
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000