import React, {Fragment} from "react";
import {CContainer, CRow} from "@coreui/react";


const Agenda = (props) => {

    return (
        <Fragment>
        </Fragment>
    );
};

export default Agenda;